---
interface Props {
  text?: string;
  class?: string;
}

const { text, class: className } = Astro.props;
---

<span class:list={[
  'text-xs font-semibold text-[#C9A84C] border border-[#C9A84C]/30 rounded-full px-3 py-1 bg-[#C9A84C]/5',
  className,
]}>
  {text ?? <slot />}
</span>
