---
interface Props {
  tag?: 'h1' | 'h2' | 'h3';
  class?: string;
}

const { tag: Tag = 'h2', class: className } = Astro.props;
---

<Tag class:list={[
  'text-3xl md:text-4xl font-bold text-[#F0F0F0] mb-4 leading-tight',
  className,
]}>
  <slot />
</Tag>
